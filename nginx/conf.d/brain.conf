# Brain AI 生产环境 Nginx 配置

# 后端上游
upstream backend {
    server backend:8000;
    keepalive 32;
}

# 前端上游
upstream frontend {
    server frontend:80;
    keepalive 32;
}

# HTTP 服务器
server {
    listen 80;
    server_name _;  # 替换为你的域名，如: yourdomain.com www.yourdomain.com

    # 如果你想强制 HTTPS，取消下面的注释
    # return 301 https://$server_name$request_uri;

    # 日志
    access_log /var/log/nginx/brain-access.log;
    error_log /var/log/nginx/brain-error.log;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # 后端 API 路由
    location /api/ {
        proxy_pass http://backend/api/;
        proxy_http_version 1.1;

        # 请求头
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时配置
        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;

        # WebSocket 和 SSE 支持
        proxy_set_header Connection '';
        proxy_buffering off;
        proxy_cache off;
        chunked_transfer_encoding on;

        # CORS (如果后端没有处理)
        # add_header 'Access-Control-Allow-Origin' '*' always;
        # add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
        # add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization' always;
    }

    # 健康检查
    location /api/health {
        proxy_pass http://backend/health;
        access_log off;
    }

    # 前端静态文件
    location / {
        proxy_pass http://frontend/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS 服务器 (如果使用 SSL)
# server {
#     listen 443 ssl http2;
#     server_name yourdomain.com www.yourdomain.com;
#
#     # SSL 证书
#     ssl_certificate /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#
#     # SSL 配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # 日志
#     access_log /var/log/nginx/brain-ssl-access.log;
#     error_log /var/log/nginx/brain-ssl-error.log;
#
#     # 安全头
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#
#     # 后端 API 路由
#     location /api/ {
#         proxy_pass http://backend/api/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 300s;
#         proxy_read_timeout 300s;
#         proxy_set_header Connection '';
#         proxy_buffering off;
#         proxy_cache off;
#         chunked_transfer_encoding on;
#     }
#
#     # 健康检查
#     location /api/health {
#         proxy_pass http://backend/health;
#         access_log off;
#     }
#
#     # 前端静态文件
#     location / {
#         proxy_pass http://frontend/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
